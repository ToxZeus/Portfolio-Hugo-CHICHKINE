{
  "name": "portfolio-hugo-chichkine",
  "version": "1.0.0",
  "private": true,
  "description": "Modern frontend tooling for Hugo Chichkine's portfolio",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2"
  }
}